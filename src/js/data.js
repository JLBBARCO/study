const JSON_LOCAL_PATH = "data.json";

function JSONData() {
  const body = document.querySelector("body");
  const main = document.querySelector("main");
  const aside = document.querySelector("aside");
  if (!body) {
    return;
  }
}
